import Vue from 'vue';
import Vuetify from 'vuetify'
import App from './App.vue';

Vue.use(Vuetify);

/* eslint-disable no-new */
new Vue({
  el: '#app',
  template: '<App/>',
  components: { App }
});
